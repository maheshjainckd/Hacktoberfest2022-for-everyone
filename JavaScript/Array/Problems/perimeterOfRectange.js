// Function to return the perimeter of the rectangle when length and breadth are passed as arguments

function perimeterRectangle(length, breadth) {
    return 2 * (length + breadth);
}

console.log('Perimeter of the rectangle is ' + perimeterRectangle(2, 18) + ' cm');